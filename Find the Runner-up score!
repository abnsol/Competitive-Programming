if __name__ == '__main__':
    n = int(input())
    arr = list(map(int, input().split()))
    
    high_score = max(arr)
    runner_up = -1
    for score in arr:
        if score > runner_up and score != high_score:
            runner_up = score
            
    print(runner_up)
